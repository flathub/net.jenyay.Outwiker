app-id: net.jenyay.Outwiker
runtime: org.gnome.Platform
runtime-version: '42'
sdk: org.gnome.Sdk
command: outwiker
rename-icon: net.jenyay.Outwiker
finish-args:
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
  - --share=ipc
  - --device=dri
  - --socket=pulseaudio
  - --filesystem=host
modules:
  - name: outwiker
    buildsystem: simple
    no-autogen: true
    sources:
      - type: git
        url: https://github.com/Jenyay/outwiker
        commit: feadfac6ce50ab1009243b0bf8c04baaad0bbe5f
    build-commands:
      - make -f Makefile.flatpak install
  - "python3-numpy.json"
  - "python3-requirements.json"
  - name: cacheman
    buildsystem: simple
    build-commands:
      - "python -m pip install . --prefix=${FLATPAK_DEST}"
    sources:
      - type: git
        url: https://github.com/Jenyay/py_cache_manager
        commit: aa7fa33dcbc30dbd6b2e62cade5f371f6dbbe2cb
  - name: cyhunspell
    buildsystem: simple
    build-commands:
      - "python -m pip install . --prefix=${FLATPAK_DEST}"
    sources:
      - type: git
        url: https://github.com/Jenyay/cython_hunspell
        commit: 399483cd722c4940f6cbac5aaf30fb078713f524
